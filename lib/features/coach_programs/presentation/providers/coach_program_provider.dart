/// Coach Program Provider
///
/// The old `CoachProgramsNotifier` and `ProgramDetailNotifier` managed the
/// now-dropped `program` table. They have been removed. Client program
/// mutations are handled by `ProgramBuilderProvider` (Phase 2).
///
/// This file is intentionally left as a placeholder to keep the barrel
/// export stable. It will be deleted in Phase 4 cleanup.
library;
